# template-name: Экспорт модели# template-type: Экспорт модели# <code-block> Export modelmodel.save('path/to/location',  # String, PathLike, path to SavedModel or H5 file           overwrite=True,  # Whether to silently overwrite any existing file at the target location           include_optimizer=True,  # If True, save optimizer's state together           save_format=None,  # 'tf' or 'h5'           signatures=None,           options=None,  # tf.saved_model.SaveOptions           save_traces=True)  # store the function traces for each layer; it requires that all custom layers/models implement a get_config() method# import model:#     import tensorflow as tf#     model = tf.keras.models.load_model('path/to/location',  # String, pathlib.Path or h5py.File#                                        custom_objects=None,  # Optional dictionary mapping names (strings) to custom classes or functions to be considered during deserialization#                                        compile=True,  # compile the model after loading#                                        options=None)  # tf.saved_model.LoadOptions